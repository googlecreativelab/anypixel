{
	"name": "iridescent-life",
	"display_name": "Iridescent Life",
	"description": "Press multiple buttons, hold the pattern, and release all at the same time",
	"version": "1.1.0",
	"license": "Apache-2.0",
	"main": "./index.js",
	"scripts": {
		"build": "browserify app.js > index.js",
		"postinstall": "browserify app.js > index.js"
	},
	"dependencies": {
    		"anypixel": "^1.0.0"
	}
}
